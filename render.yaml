services:
  - type: web
    name: mohd-adeeb-portfolio
    runtime: static
    buildCommand: npm run render-build
    staticPublishPath: ./out
    envVars:
      - key: NODE_VERSION
        value: 20
